name: ⚡ Quick Test

on:
  workflow_dispatch:  # 수동 실행만 가능

jobs:
  quick-test:
    name: "⚡ Quick Functionality Test"
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: "🏰 Test GnFortress Scanner"
        uses: ./
        with:
          image: 'nginx:latest'
          
      - name: "🎉 Test completed!"
        run: echo "✅ Basic functionality working!"